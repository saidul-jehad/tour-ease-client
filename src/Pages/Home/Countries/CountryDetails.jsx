// import { useParams } from "react-router-dom";

const CountryDetails = () => {
    // const id = useParams()
    return (
        <div>
            Details
        </div>
    );
};

export default CountryDetails;